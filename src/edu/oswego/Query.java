package edu.oswego;

public class Query {

    String query;

    public Query(String query){
        System.out.println("are you?");
        this.query = query;
    }

    //takes a query and outputs ranked results in array of some sort

    //uses mullti-threading probably a local threaded class inside cashed executor to split the work
}
